// `rail` — the reference layout: wide narrative column, a coloured information
// rail on the right, and a portrait well at the top of the rail. This is the
// default template and the one whose output claims the unsuffixed filenames
// the site serves.
#import "../lib/two-column.typ"

#let render(ctx) = two-column.render(ctx)
